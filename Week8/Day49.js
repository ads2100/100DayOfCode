// there is different ways to add the element to the array

// either 
var numbers1 = []; // we create empty array
numbers1[0] = 'zero'; // then we add the first element to array
numbers1[1] = 'one'; // we add the second element to array
console.log(numbers1);

// or 

numbers2 = []; 
numbers.push('zero') // we add the first element to array
numbers.push('one', 'two') // we add the second and the third element to array
console.log(numbers2);

// or 

var names = ['Shahad', 'Amal', 'Aisha']; // we create array with three elements
names[3] = 'Asma';
names[4] = 'kholoud';
console.log(names);